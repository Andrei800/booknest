# 🚀 Деплой BookNest на Render.com (бесплатно)

## Шаг 1: Подготовка репозитория

1. Создайте аккаунт на [GitHub](https://github.com) (если нет)
2. Создайте новый репозиторий `booknest`
3. Загрузите проект:

```bash
cd "c:\Users\Admin\OneDrive\Рабочий стол\Разработка\MyLibraryAI"
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/booknest.git
git push -u origin main
```

## Шаг 2: Деплой на Render

1. Зайдите на [render.com](https://render.com) и создайте аккаунт (через GitHub)
2. Нажмите **New** → **Web Service**
3. Подключите ваш GitHub репозиторий `booknest`
4. Настройки:
   - **Name**: `booknest`
   - **Region**: Frankfurt (EU Central) или Oregon (US West)
   - **Branch**: `main`
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT`
   
5. Выберите **Free** план
6. Нажмите **Create Web Service**

## Шаг 3: Добавление диска для базы данных

⚠️ **Важно**: На бесплатном плане Render диск не доступен. База будет сбрасываться при перезапуске.

**Варианты решения:**
1. **Upgrade до Starter ($7/мес)** — добавьте Disk в настройках
2. **Использовать облачную БД** — [Supabase](https://supabase.com) (бесплатно)
3. **Экспорт/Импорт** — периодически скачивать JSON через приложение

## Шаг 4: Переменные окружения

В настройках сервиса → **Environment** добавьте:
- `GEMINI_API_KEY` = ваш ключ Gemini (для AI рекомендаций)

## Готово! 🎉

Ваше приложение будет доступно по адресу:
`https://booknest.onrender.com`

---

## Альтернатива: PythonAnywhere (не спит!)

1. Зарегистрируйтесь на [pythonanywhere.com](https://www.pythonanywhere.com)
2. **Web** → **Add a new web app** → **Flask** (работает и с FastAPI)
3. Загрузите файлы через **Files**
4. Настройте WSGI файл

Преимущество: сайт не "засыпает", но ограничен трафик.

---

## Локальный доступ через интернет (Cloudflare Tunnel)

Бесплатный способ сделать ваш локальный сервер доступным из интернета:

1. Скачайте [cloudflared](https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/)
2. Запустите туннель:
```bash
cloudflared tunnel --url http://localhost:8000
```
3. Получите публичный URL (работает пока запущен компьютер)
